const footerLinkColor = '#ACACAC'

const style = () => ({
    footerSocialIcon: {
        color: footerLinkColor,
        transition: 'all 150ms ease-in',
        '&:hover,&:focus': {
            color: '#FFFFFF',
        },
    },
})

export default style